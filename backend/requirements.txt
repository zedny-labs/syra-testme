# ── Core framework ────────────────────────────
# Pinned: Starlette 1.x (pulled in by fastapi >=0.13x) changed nested-router path
# registration, which breaks the startup _assert_required_api_routes() check and
# prevents the backend from booting. fastapi 0.115.6 constrains starlette to 0.41.x.
# Verified: these versions register all REQUIRED_API_ROUTES; newer ones do not.
fastapi==0.115.6
starlette==0.41.3
uvicorn>=0.29.0
gunicorn>=22.0.0
websockets>=12.0
slowapi>=0.1.9
python-multipart>=0.0.9

# ── Database ──────────────────────────────────
sqlalchemy>=2.0
psycopg[binary]>=3.1
alembic>=1.13

# ── Authentication ────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0,<5

# ── HTTP client ───────────────────────────────
httpx>=0.27.0
celery[redis]>=5.4.0
redis[asyncio]>=4.2.0          # live_bus.py uses redis.asyncio for cross-worker pub/sub

# ── Config & templating ──────────────────────
# Pinned alongside fastapi/starlette above for a reproducible, verified build.
pydantic-settings==2.7.1
pydantic[email]==2.10.4
jinja2>=3.1.3
pyyaml>=6.0

# ── ML / AI Proctoring ──────────────────────
opencv-python>=4.9.0
mediapipe>=0.10.9,<0.10.30
ultralytics>=8.1.0
numpy>=1.26.0
deepface>=0.0.93
tf-keras
webrtcvad-wheels>=2.0.14
scipy>=1.11.0

# ── Observability ────────────────────────────
sentry-sdk[fastapi]>=2.0.0

# ── Utilities ────────────────────────────────
reportlab>=4.1.0
croniter>=2.0.0
pytesseract>=0.3.10
bleach>=6.1.0
openai>=1.12.0
